C++ Exercise: String 1

1. Create a String class (if the compiler complains it already has one, call
	your class MyString).
2. Required functionality:
	GetLength()
	Assign(otherString)
	CharAt(index)
	Append(otherString)
	Compare(otherString)
	IsEmpty()
	Clear()